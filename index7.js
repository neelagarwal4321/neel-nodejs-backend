// complete practice

const express = require('express');

const app = express();

app.use(express.json());

app.get("/members", async(req, res) => {
    try{

    }
    catch(error){
        
    }
});